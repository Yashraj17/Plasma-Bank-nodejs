<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Plasma Bank</title>
</head>
<body>
    <%- include('base') -%>
    <div class="container mt-3">
            <div class="row">
                <div class="col-12 col-lg-6">
                    <div class="card bg-success text-white">
                        <div class="card-body">
                            <h2 class="display-5 mb-0"><%- donor %></h2>
                            <h4 class="lead">Total Donors</h4>
                            <a href="/manageDonor" class="btn btn-light float-end">View Donors</a>
                        </div>
                    </div>
                </div>
                <div class="col-12 col-lg-6">
                    <div class="card bg-warning text-white">
                        <div class="card-body">
                            <h2 class="display-5 mb-0"><%- request %></h2>
                            <h4 class="lead">Total Requesters</h4>
                            <a href="/manageRequester" class="btn btn-light float-end">View Requests</a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row mt-3 d-block-inline">
                <% data.forEach(s => { %>

                    <div class="col-3 my-1">
                        <a href="" class="btn @if($loop->even) btn-primary @else btn-info @endif py-3 w-100">
                            <option value="<%-s._id %>"><%- s.name_of_state %></option>
                            <p class="small m-0">Total Donors:<%- s.no_of_donor %> </p>
                            <p class="small m-0">Total Requesters:<%- s.no_of_request%> </p>
                        </a>
                    </div>
                   
                <% }) %>
               
                
        </div>
    </div>
</body>
</html>